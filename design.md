# Personal Archive Design Language (Frontier Optimism × Bento)

> 一套专为个人博客/数字档案库定制的前端设计规范。
> 结合了「Frontier Optimism (科技乐观主义)」的情绪内核与「Bento Grid (便当盒网格)」的结构秩序，旨在将个人的思考与写作呈现为一个“充满温度的科学仪器”。

## 1. 设计意图 (Design Intent)

这个博客不应该像一个传统的“信息流”，而应该像一个**控制台（Dashboard）**或**档案库（Archive）**。
视觉上需要同时传达两种截然不同的力量，并让它们保持平衡：
- **工程的极致精确**：1px 的细线框、等宽字体的数据标签、状态指示器、雷达图谱、严丝合缝的网格切分。
- **人文的温度与呼吸**：暖调的纸张背景色、圆润的外部容器包裹、充足的留白、自然平滑的转场动画。

## 2. 空间与布局 (Layout System)

放弃传统的无边界瀑布流，采用**边界明确的 Bento Box（便当盒）结构**。

- **主容器 (The Vessel)**：页面容器铺满整个浏览器视口，不设置最大宽度、外围留白或外部圆角。Bento 分区通过内部 `1px` 边界维持“精密仪器”的结构感，并在超宽屏上自然扩展。
- **切分 (Subdivision)**：
  - **Header / Footer**：顶部的导航栏与底部的数据状态栏（LATENT SPACE / SIGNAL STRENGTH）构成仪器的外壳框架。
  - **Hero 区块 (60 / 40 比例)**：左侧为主视觉与行动号召（60%），右侧进一步拆分为观察（雷达）、连接与构建等辅助说明模块（40%）。
  - **底纹 (Motifs)**：大面积使用 `20px` 间距的点阵底纹（Dot Grid），强化工程图纸的隐喻。

## 3. 色彩系统 (Color Palette)

保持高度克制，依赖黑白灰（暖色相）建立结构，只用单一高饱和色进行状态提示。

- **背景主色 (Paper)**: `#f3f0e8` - 温暖的纸白色，避免刺眼的纯白。
- **背景辅色 (Mist)**: `#d9d9d2` - 略深的灰色，用于区分不同的功能区块。
- **文字与边框 (Ink)**: `#0b0d10` - 深墨色，用于主文本、1px 细线边框。
- **辅助白色 (White)**: `#ffffff` - 仅作为局部高对比背景使用，不在主容器外保留固定留白。
- **信号色 (Orbit Blue)**: `#3568ff` - 高纯度克莱因蓝，仅用于 Hover 状态、进度指示器、分类标签和高亮信号。

## 4. 字体与排版 (Typography)

利用极端的字号对比（Extreme Scale Contrast）制造张力。

- **展示字体 (Display)**: `Space Grotesk`。用于巨大的主标题（如“看见结构，建立连接”），字号可达 `6rem`，行高极度紧凑（`0.9`），字距收紧，制造雕塑感。
- **正文字体 (Body)**: `Inter`。用于正文和长文阅读，保证易读性，行高宽松（`1.8`），字重适中。
- **数据与标签 (Data)**: `IBM Plex Mono`。无处不在的小字号等宽字体（`0.55rem - 0.65rem`），全部大写，字距拉开（tracking-widest）。用于坐标、编号、日期、状态码（如 `00 // CONCEPT`, `STATUS: READY`），是建立“科技感”的核心手段。

## 5. 组件与交互规则 (Components & Interactions)

### 5.1 无缝模式切换 (Seamless Modes)
系统被划分为两个核心状态，由 `AnimatePresence` 驱动平滑过渡：
1. **仪表盘模式 (Dashboard Mode)**：高度结构化的 Bento 布局，展示站点愿景与文章列表（Archive Feed）。
2. **阅读模式 (Reading Mode)**：点击文章后，仪表盘模块淡出，进入沉浸式阅读视图。顶部保留吸顶导航（Return to Archive），正文宽度严格限制在 `65ch`，保证视线移动舒适。

### 5.2 微交互 (Micro-interactions)
- **Hover 反馈**：文章列表的 Hover 状态不使用沉重的阴影，而是使用极度克制的背景色微调（`bg-mist/30`），并将标题颜色平滑过渡为 `Orbit Blue`。
- **装饰线与节点**：底部的信号强度条、雷达图中的同心圆，都是无实际功能但极强的情绪锚点（Emotional Anchors），丰富了“精密仪器”的设定。

## 6. 博客功能映射 (Blog Function Mapping)

- **主页愿景** -> Bento Hero 区域，传达个人/站点的核心价值观。
- **文章分类** -> `IBM Plex Mono` 标签（如 ESSAY, ENGINEERING）。
- **文章列表** -> `04 // JOURNAL ARCHIVE` 模块，紧跟在 Hero 下方，采用简洁的横向列表排版，不使用卡片，仅用细线（border-bottom）分隔。
- **阅读页** -> 独立的视图组件，隐藏无关干扰，保留元数据（日期、分类）在标题旁作为锚点。

## 7. 避坑指南 (Anti-patterns)

- **不要使用卡片投影**：内部的模块切分必须依赖 `1px` 的实线边框（`border-line-dark`），绝对不要在内部模块使用阴影（Shadows），这会破坏工程图纸的平面感。
- **不要堆砌渐变色**：全局禁用任何形式的背景渐变色、紫蓝 AI 色。光影仅通过大色块的明暗对比来实现。
- **避免臃肿的动效**：动效（Framer Motion）主要用于页面状态的切换（淡入淡出、轻微位移），禁止一切无意义的弹跳（Bouncy）和视差滚动。
